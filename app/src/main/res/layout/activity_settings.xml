<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.studios.currencyconverter.SettingsActivity"
    android:layout_margin="5dp"
    >

    <TextView
        android:id="@+id/choose_main_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentStart="true"
        android:textSize="15dp"
        android:text="Основная валюта"
        />

    <android.support.v7.widget.AppCompatSpinner
        android:id="@+id/main_spinner"
        android:layout_toRightOf="@+id/choose_main_text"
        android:layout_marginLeft="100dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:textAlignment="textEnd"
        >

    </android.support.v7.widget.AppCompatSpinner>

    <ListView
        android:id="@+id/currency_list_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@+id/main_spinner"
        android:layout_marginTop="30dp"
        android:layout_above="@+id/save_button"
        ></ListView>

    <Button
        android:id="@+id/save_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentEnd="true"
        android:text="Сохранить"
        android:textAllCaps="false"
        />

</RelativeLayout>
